<!-- <!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Invoice <%= invoice.invoiceNumber %></title>
  <style>
    :root {
      --primary: #64748b;
      --primary-dark: #475569;
      --primary-light: #cbd5e1;
      --accent: #94a3b8;
      --text: #0f172a;
      --text-light: #64748b;
      --border: #e2e8f0;
    }
    
    * { margin: 0; padding: 0; box-sizing: border-box; }
    
    body { 
      font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
      color: var(--text);
      background: white;
      /* padding: 50px 30px; */
      padding: 20px;
      font-size: 14px;
      line-height: 1.6;
    }
    
    .container { 
      max-width: 750px; 
      margin: 0 auto;
    }
    
    .header {
      margin-bottom: 60px;
    }
    
    .invoice-title {
      font-size: 14px;
      text-transform: uppercase;
      letter-spacing: 3px;
      color: var(--text-light);
      font-weight: 400;
      margin-bottom: 5px;
    }
    
    .invoice-number {
      font-size: 48px;
      font-weight: 300;
      color: var(--text);
      letter-spacing: -2px;
      margin-bottom: 30px;
    }
    
    .divider {
      height: 1px;
      background: var(--border);
      margin: 40px 0;
    }
    
    .divider-thick {
      height: 2px;
      background: var(--primary-dark);
    }
    
    .row {
      display: flex;
      justify-content: space-between;
      margin-bottom: 40px;
    }
    
    .col {
      flex: 1;
    }
    
    .label {
      font-size: 11px;
      text-transform: uppercase;
      letter-spacing: 1.5px;
      color: var(--text-light);
      font-weight: 600;
      margin-bottom: 12px;
    }
    
    .company-name {
      font-size: 18px;
      font-weight: 600;
      color: var(--text);
      margin-bottom: 6px;
      letter-spacing: -0.5px;
    }
    
    .info-text {
      font-size: 13px;
      color: var(--text-light);
      line-height: 1.8;
    }
    
    .dates-row {
      display: flex;
      gap: 40px;
      margin-bottom: 40px;
    }
    
    .date-item {
      flex: 1;
    }
    
    .date-label {
      font-size: 11px;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: var(--text-light);
      margin-bottom: 6px;
    }
    
    .date-value {
      font-size: 13px;
      color: var(--text);
      font-weight: 500;
    }
    
    .status {
      display: inline-block;
      padding: 4px 12px;
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-weight: 600;
      border: 1px solid var(--border);
      border-radius: 3px;
      color: var(--text-light);
    }
    
    table {
      width: 100%;
      border-collapse: collapse;
      margin: 40px 0;
    }
    
    thead th {
      padding: 15px 0;
      text-align: left;
      font-size: 11px;
      text-transform: uppercase;
      font-weight: 600;
      letter-spacing: 1.5px;
      color: var(--text-light);
      border-bottom: 2px solid var(--primary-dark);
    }
    
    thead th.right { text-align: right; }
    thead th.center { text-align: center; }
    
    tbody td {
      padding: 20px 0;
      font-size: 13px;
      color: var(--text);
      border-bottom: 1px solid var(--border);
    }
    
    tbody td:first-child {
      font-weight: 500;
    }
    
    tbody td.right { 
      text-align: right; 
      font-weight: 500;
      font-variant-numeric: tabular-nums;
    }
    
    tbody td.center { 
      text-align: center;
      color: var(--text-light);
    }
    
    .totals {
      margin-top: 40px;
      display: flex;
      justify-content: flex-end;
    }
    
    .totals-wrapper {
      width: 300px;
    }
    
    .total-row {
      display: flex;
      justify-content: space-between;
      padding: 12px 0;
      font-size: 13px;
      color: var(--text-light);
    }
    
    .total-row span:last-child {
      font-weight: 500;
      font-variant-numeric: tabular-nums;
      color: var(--text);
    }
    
    .total-final {
      border-top: 2px solid var(--primary-dark);
      margin-top: 15px;
      padding-top: 20px;
      font-size: 20px;
      font-weight: 600;
      color: var(--text);
    }
    
    .notes {
      margin-top: 60px;
      padding-top: 30px;
      border-top: 1px solid var(--border);
    }
    
    .notes-label {
      font-size: 11px;
      text-transform: uppercase;
      letter-spacing: 1.5px;
      color: var(--text-light);
      font-weight: 600;
      margin-bottom: 12px;
    }
    
    .notes-text {
      font-size: 13px;
      color: var(--text-light);
      line-height: 1.8;
      max-width: 600px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="invoice-title">Invoice</div>
      <div class="invoice-number">#<%= invoice.invoiceNumber %></div>
      <div class="status"><%= invoice.status %></div>
    </div>

    <div class="row">
      <div class="col">
        <div class="label">From</div>
        <div class="company-name"><%= invoice.billFrom?.businessName || 'Your Business' %></div>
        <div class="info-text">
          <%= invoice.billFrom?.address || '' %><br/>
          <%= invoice.billFrom?.email || '' %><br/>
          <%= invoice.billFrom?.phone || '' %>
        </div>
      </div>
      
      <div class="col">
        <div class="label">To</div>
        <div class="company-name"><%= invoice.billTo?.clientName || '' %></div>
        <div class="info-text">
          <%= invoice.billTo?.address || '' %><br/>
          <%= invoice.billTo?.email || '' %><br/>
          <%= invoice.billTo?.phone || '' %>
        </div>
      </div>
    </div>

    <div class="divider"></div>

    <div class="dates-row">
      <div class="date-item">
        <div class="date-label">Issued</div>
        <div class="date-value"><%= new Date(invoice.invoiceDate).toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' }) %></div>
      </div>
      
      <div class="date-item">
        <div class="date-label">Due</div>
        <div class="date-value"><%= new Date(invoice.dueDate).toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' }) %></div>
      </div>
      
      <div class="date-item">
        <div class="date-label">Terms</div>
        <div class="date-value"><%= invoice.paymentTerms || 'Net 30' %></div>
      </div>
    </div>

    <table>
      <thead>
        <tr>
          <th>Description</th>
          <th class="center">Qty</th>
          <th class="right">Rate</th>
          <th class="right">Amount</th>
        </tr>
      </thead>
      <tbody>
        <% (invoice.items || []).forEach(function(item){ %>
          <tr>
            <td><%= item.name %></td>
            <td class="center"><%= item.quantity %></td>
            <td class="right">$<%= Number(item.unitPrice || 0).toFixed(2) %></td>
            <td class="right">$<%= Number(item.total || 0).toFixed(2) %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>

    <div class="totals">
      <div class="totals-wrapper">
        <div class="total-row">
          <span>Subtotal</span>
          <span>$<%= Number(invoice.subTotal||0).toFixed(2) %></span>
        </div>
        <div class="total-row">
          <span>Tax</span>
          <span>$<%= Number(invoice.taxTotal||0).toFixed(2) %></span>
        </div>
        <div class="total-row total-final">
          <span>Total</span>
          <span>$<%= Number(invoice.total||0).toFixed(2) %></span>
        </div>
      </div>
    </div>

    <% if (invoice.notes) { %>
      <div class="notes">
        <div class="notes-label">Notes</div>
        <div class="notes-text"><%= invoice.notes %></div>
      </div>
    <% } %>
  </div>
</body>
</html> -->

<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Invoice <%= invoice.invoiceNumber %></title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    
    body { 
      font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
      color: #0f172a;
      background: white;
      padding: 25px 20px;
      font-size: 10px;
      line-height: 1.4;
    }
    
    .container { 
      max-width: 700px; 
      margin: 0 auto;
    }
    
    .header {
      margin-bottom: 35px;
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
    }
    
    .logo-area {
      display: flex;
      align-items: center;
      gap: 12px;
    }
    
    .logo {
      width: 50px;
      height: 50px;
      object-fit: contain;
    }
    
    .invoice-title {
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 2px;
      color: #64748b;
      font-weight: 400;
      margin-bottom: 3px;
    }
    
    .invoice-number {
      font-size: 28px;
      font-weight: 300;
      color: #0f172a;
      letter-spacing: -1px;
      margin-bottom: 20px;
    }
    
    .divider {
      height: 1px;
      background: #e2e8f0;
      margin: 25px 0;
    }
    
    .divider-thick {
      height: 1px;
      background: <%= colors.primaryDark %>;
    }
    
    .row {
      display: flex;
      justify-content: space-between;
      margin-bottom: 25px;
    }
    
    .col {
      flex: 1;
    }
    
    .label {
      font-size: 9px;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: #64748b;
      font-weight: 600;
      margin-bottom: 8px;
    }
    
    .company-name {
      font-size: 13px;
      font-weight: 600;
      color: #0f172a;
      margin-bottom: 4px;
      letter-spacing: -0.3px;
    }
    
    .info-text {
      font-size: 10px;
      color: #64748b;
      line-height: 1.6;
    }
    
    .dates-row {
      display: flex;
      gap: 25px;
      margin-bottom: 25px;
    }
    
    .date-item {
      flex: 1;
    }
    
    .date-label {
      font-size: 9px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      color: #64748b;
      margin-bottom: 4px;
    }
    
    .date-value {
      font-size: 10px;
      color: #0f172a;
      font-weight: 500;
    }
    
    .status {
      display: inline-block;
      padding: 3px 10px;
      font-size: 9px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      font-weight: 600;
      border: 1px solid #e2e8f0;
      border-radius: 3px;
      color: #64748b;
    }
    
    table {
      width: 100%;
      border-collapse: collapse;
      margin: 25px 0;
    }
    
    thead th {
      padding: 10px 0;
      text-align: left;
      font-size: 9px;
      text-transform: uppercase;
      font-weight: 600;
      letter-spacing: 1px;
      color: #64748b;
      border-bottom: 1px solid <%= colors.primaryDark %>;
    }
    
    thead th.right { text-align: right; }
    thead th.center { text-align: center; }
    
    tbody td {
      padding: 12px 0;
      font-size: 10px;
      color: #0f172a;
      border-bottom: 1px solid #e2e8f0;
    }
    
    tbody td:first-child {
      font-weight: 500;
    }
    
    tbody td.right { 
      text-align: right; 
      font-weight: 500;
      font-variant-numeric: tabular-nums;
    }
    
    tbody td.center { 
      text-align: center;
      color: #64748b;
    }
    
    .totals {
      margin-top: 25px;
      display: flex;
      justify-content: flex-end;
    }
    
    .totals-wrapper {
      width: 220px;
    }
    
    .total-row {
      display: flex;
      justify-content: space-between;
      padding: 8px 0;
      font-size: 10px;
      color: #64748b;
    }
    
    .total-row span:last-child {
      font-weight: 500;
      font-variant-numeric: tabular-nums;
      color: #0f172a;
    }
    
    .total-final {
      border-top: 1px solid <%= colors.primaryDark %>;
      margin-top: 10px;
      padding-top: 12px;
      font-size: 14px;
      font-weight: 600;
      color: #0f172a;
    }
    
    .notes {
      margin-top: 35px;
      padding-top: 20px;
      border-top: 1px solid #e2e8f0;
    }
    
    .notes-label {
      font-size: 9px;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: #64748b;
      font-weight: 600;
      margin-bottom: 8px;
    }
    
    .notes-text {
      font-size: 10px;
      color: #64748b;
      line-height: 1.6;
      max-width: 550px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="logo-area">
        <% if (businessLogo) { %>
          <img src="<%= businessLogo %>" alt="Logo" class="logo" />
        <% } %>
        <div>
          <div class="invoice-title">Invoice</div>
          <div class="invoice-number">#<%= invoice.invoiceNumber %></div>
          <div class="status"><%= invoice.status %></div>
        </div>
      </div>
    </div>

    <div class="row">
      <div class="col">
        <div class="label">From</div>
        <div class="company-name"><%= invoice.billFrom?.businessName || 'Your Business' %></div>
        <div class="info-text">
          <%= invoice.billFrom?.address || '' %><br/>
          <%= invoice.billFrom?.email || '' %><br/>
          <%= invoice.billFrom?.phone || '' %>
        </div>
      </div>
      
      <div class="col">
        <div class="label">To</div>
        <div class="company-name"><%= invoice.billTo?.clientName || '' %></div>
        <div class="info-text">
          <%= invoice.billTo?.address || '' %><br/>
          <%= invoice.billTo?.email || '' %><br/>
          <%= invoice.billTo?.phone || '' %>
        </div>
      </div>
    </div>

    <div class="divider"></div>

    <div class="dates-row">
      <div class="date-item">
        <div class="date-label">Issued</div>
        <div class="date-value"><%= new Date(invoice.invoiceDate).toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' }) %></div>
      </div>
      
      <div class="date-item">
        <div class="date-label">Due</div>
        <div class="date-value"><%= new Date(invoice.dueDate).toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' }) %></div>
      </div>
      
      <div class="date-item">
        <div class="date-label">Terms</div>
        <div class="date-value"><%= invoice.paymentTerms || 'Net 30' %></div>
      </div>
    </div>

    <table>
      <thead>
        <tr>
          <th>Description</th>
          <th class="center">Qty</th>
          <th class="right">Rate</th>
          <th class="right">Amount</th>
        </tr>
      </thead>
      <tbody>
        <% (invoice.items || []).forEach(function(item){ %>
          <tr>
            <td><%= item.name %></td>
            <td class="center"><%= item.quantity %></td>
            <td class="right">$<%= Number(item.unitPrice || 0).toFixed(2) %></td>
            <td class="right">$<%= Number(item.total || 0).toFixed(2) %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>

    <div class="totals">
      <div class="totals-wrapper">
        <div class="total-row">
          <span>Subtotal</span>
          <span>$<%= Number(invoice.subTotal||0).toFixed(2) %></span>
        </div>
        <div class="total-row">
          <span>Tax</span>
          <span>$<%= Number(invoice.taxTotal||0).toFixed(2) %></span>
        </div>
        <div class="total-row total-final">
          <span>Total</span>
          <span>$<%= Number(invoice.total||0).toFixed(2) %></span>
        </div>
      </div>
    </div>

    <% if (invoice.notes) { %>
      <div class="notes">
        <div class="notes-label">Notes</div>
        <div class="notes-text"><%= invoice.notes %></div>
      </div>
    <% } %>
  </div>
</body>
</html>