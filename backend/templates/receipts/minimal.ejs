<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Receipt <%= receipt.receiptNumber %></title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    
    /* CSS variables driven by Settings.branding.primaryColor (or colors fallback) */
    :root {
      --brand: <%= (typeof brandColor !== 'undefined' && brandColor) ? brandColor : ((colors && colors.primary) ? colors.primary : '#64748b') %>;
      --brand-dark: <%= (colors && colors.primaryDark) ? colors.primaryDark : '#475569' %>;
      --brand-light: <%= (colors && colors.primaryLight) ? colors.primaryLight : '#cbd5e1' %>;
      --brand-lighter: <%= (colors && colors.primaryLighter) ? colors.primaryLighter : '#e6eef8' %>;
      --brand-pale: <%= (colors && colors.primaryPale) ? colors.primaryPale : '#f8fafc' %>;
      --text: <%= (colors && colors.text) ? colors.text : '#0f172a' %>;
      --text-light: <%= (colors && colors.textLight) ? colors.textLight : '#64748b' %>;
      --border: <%= (colors && colors.border) ? colors.border : '#e2e8f0' %>;
    }

    body { 
      font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
      color: #0f172a;
      background: white;
      padding: 25px 20px;
      font-size: 10px;
      line-height: 1.4;
    }
    
    .container { 
      max-width: 700px; 
      margin: 0 auto;
    }
    
    .header {
      margin-bottom: 35px;
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
    }
    
    .logo-area {
      display: flex;
      align-items: center;
      gap: 12px;
    }
    
    .logo {
      width: 50px;
      height: 50px;
      object-fit: contain;
    }
    
    .receipt-title {
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 2px;
      color: #64748b;
      font-weight: 400;
      margin-bottom: 3px;
    }
    
    .receipt-number {
      font-size: 28px;
      font-weight: 300;
      color: #0f172a;
      letter-spacing: -1px;
      margin-bottom: 20px;
    }
    
    .divider {
      height: 1px;
      background: #e2e8f0;
      margin: 25px 0;
    }
    
    .divider-thick {
      height: 1px;
      background: var(--brand-dark);
    }
    
    .row {
      display: flex;
      justify-content: space-between;
      margin-bottom: 25px;
    }
    
    .col {
      flex: 1;
    }
    
    .label {
      font-size: 9px;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: #64748b;
      font-weight: 600;
      margin-bottom: 8px;
    }
    
    .company-name {
      font-size: 13px;
      font-weight: 600;
      color: #0f172a;
      margin-bottom: 4px;
      letter-spacing: -0.3px;
    }
    
    .info-text {
      font-size: 10px;
      color: #64748b;
      line-height: 1.6;
    }
    
    .dates-row {
      display: flex;
      gap: 25px;
      margin-bottom: 25px;
    }
    
    .date-item {
      flex: 1;
    }
    
    .date-label {
      font-size: 9px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      color: #64748b;
      margin-bottom: 4px;
    }
    
    .date-value {
      font-size: 10px;
      color: #0f172a;
      font-weight: 500;
    }
    
    .status {
      display: inline-block;
      padding: 3px 10px;
      font-size: 9px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      font-weight: 600;
      background: #22c55e;
      border-radius: 3px;
      color: white;
    }

    .payment-details {
      background: #f0fdf4;
      padding: 12px;
      border-radius: 3px;
      margin-bottom: 25px;
      border-left: 1px solid #22c55e;
    }

    .payment-details-title {
      font-size: 9px;
      text-transform: uppercase;
      color: #166534;
      font-weight: 600;
      letter-spacing: 0.5px;
      margin-bottom: 8px;
    }

    .payment-row {
      display: flex;
      justify-content: space-between;
      padding: 4px 0;
      font-size: 10px;
      color: #166534;
    }

    .payment-row span:last-child {
      font-weight: 500;
    }
    
    table {
      width: 100%;
      border-collapse: collapse;
      margin: 25px 0;
    }
    
    thead th {
      padding: 10px 0;
      text-align: left;
      font-size: 9px;
      text-transform: uppercase;
      font-weight: 600;
      letter-spacing: 1px;
      color: #64748b;
      border-bottom: 1px solid var(--brand-dark);
    }
    
    thead th.right { text-align: right; }
    thead th.center { text-align: center; }
    
    tbody td {
      padding: 12px 0;
      font-size: 10px;
      color: #0f172a;
      border-bottom: 1px solid #e2e8f0;
    }
    
    tbody td:first-child {
      font-weight: 500;
    }
    
    tbody td.right { 
      text-align: right; 
      font-weight: 500;
      font-variant-numeric: tabular-nums;
    }
    
    tbody td.center { 
      text-align: center;
      color: #64748b;
    }
    
    .totals {
      margin-top: 25px;
      display: flex;
      justify-content: flex-end;
    }
    
    .totals-wrapper {
      width: 220px;
    }
    
    .total-row {
      display: flex;
      justify-content: space-between;
      padding: 8px 0;
      font-size: 10px;
      color: #64748b;
    }
    
    .total-row span:last-child {
      font-weight: 500;
      font-variant-numeric: tabular-nums;
      color: #0f172a;
    }
    
    .total-final {
      border-top: 1px solid var(--brand-dark);
      margin-top: 10px;
      padding-top: 12px;
      font-size: 14px;
      font-weight: 600;
      color: #0f172a;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="logo-area">
        <% if (businessLogo) { %>
          <img src="<%= businessLogo %>" alt="Logo" class="logo" />
        <% } %>
        <div>
          <div class="receipt-title">Receipt</div>
          <div class="receipt-number">#<%= receipt.receiptNumber %></div>
          <div class="status">PAID</div>
        </div>
      </div>
    </div>

    <div class="row">
      <div class="col">
        <div class="label">From</div>
        <div class="company-name"><%= receipt.billFrom?.businessName || 'Your Business' %></div>
        <div class="info-text">
          <%= receipt.billFrom?.address || '' %><br/>
          <%= receipt.billFrom?.email || '' %><br/>
          <%= receipt.billFrom?.phone || '' %>
        </div>
      </div>
      
      <div class="col">
        <div class="label">Received From</div>
        <div class="company-name"><%= receipt.billTo?.clientName || '' %></div>
        <div class="info-text">
          <%= receipt.billTo?.address || '' %><br/>
          <%= receipt.billTo?.email || '' %><br/>
          <%= receipt.billTo?.phone || '' %>
        </div>
      </div>
    </div>

    <div class="divider"></div>

    <div class="dates-row">
      <div class="date-item">
        <div class="date-label">Issued</div>
        <div class="date-value"><%= new Date(receipt.receiptDate).toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' }) %></div>
      </div>
      
      <div class="date-item">
        <div class="date-label">Paid</div>
        <div class="date-value"><%= new Date(receipt.paymentDate).toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' }) %></div>
      </div>
      
      <div class="date-item">
        <div class="date-label">Method</div>
        <div class="date-value"><%= receipt.paymentMethod || 'Card' %></div>
      </div>
    </div>

    <div class="payment-details">
      <div class="payment-details-title">Payment Details</div>
      <div class="payment-row">
        <span>Transaction ID</span>
        <span><%= receipt.transactionId || 'N/A' %></span>
      </div>
    </div>

    <table>
      <thead>
        <tr>
          <th>Description</th>
          <th class="center">Qty</th>
          <th class="right">Rate</th>
          <th class="right">Amount</th>
        </tr>
      </thead>
      <tbody>
        <% (receipt.items || []).forEach(function(item){ %>
          <tr>
            <td><%= item.name %></td>
            <td class="center"><%= item.quantity %></td>
            <td class="right">$<%= Number(item.unitPrice || 0).toFixed(2) %></td>
            <td class="right">$<%= Number(item.total || 0).toFixed(2) %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>

    <div class="totals">
      <div class="totals-wrapper">
        <div class="total-row">
          <span>Subtotal</span>
          <span>$<%= Number(receipt.subTotal||0).toFixed(2) %></span>
        </div>
        <div class="total-row">
          <span>Tax</span>
          <span>$<%= Number(receipt.taxTotal||0).toFixed(2) %></span>
        </div>
        <div class="total-row total-final">
          <span>Amount Paid</span>
          <span>$<%= Number(receipt.total||0).toFixed(2) %></span>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
